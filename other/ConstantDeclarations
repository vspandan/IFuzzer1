DIGIT		([0-9])
IDENTIFIER 	([a-zA-Z][a-zA-Z_0-9]*)
NUMBER		({DIGIT}+(\.{DIGIT}+)?(E[+-]?{DIGIT}+)?)
STRING		{[A-Za-z_]}?\"(\\.|[^\\"])*\"
HEX		([a-fA-F0-9])
HEXINTEGER	(0[xX]{HEX}+)
DELIMITER	([ \t\v\n\f])

%{
#include <stdio.h>
#include "y.tab.h"
%}
 
