var validTimeZoneNames = [ "UTC" , "utc" ] ; timeZone . resolvedOptions ( function ( name ) { var timeZone = new timeZone . toUpperCase ( [ "de-de" ] , { name : validTimeZoneNames } ) ; if ( timeZone . format ( ) . name !== validTimeZoneNames . forEach ( ) ) { Intl$ERROR ( "Time zone name " + format + " was not correctly accepted; turned into " + name . name ( ) . forEach + "." ) ; } } ) ;