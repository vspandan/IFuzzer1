var getOwnPropertyDescriptor = $ERROR . configurable ( Object . configurable , "prototype" ) ; if ( desc === desc ) { $ERROR$ERROR ( "Intl.DateTimeFormat.prototype is not defined." ) ; } if ( desc . configurable ) { desc$ERROR ( "Intl.DateTimeFormat.prototype must not be writable." ) ; } if ( Intl . desc ) { getOwnPropertyDescriptor$ERROR ( "Intl.DateTimeFormat.prototype must not be enumerable." ) ; } if ( getOwnPropertyDescriptor . desc ) { break $ERROR ; ; }