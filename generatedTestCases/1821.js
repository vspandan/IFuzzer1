function testcase ( ) { var <<<reservedWord: = { } ; tokenCodes . continue = 0 ; p1 . for = 1 ; <<<reservedWord: . switch = 2 ; var arr = [ 'continue' , 'for' , 'switch' ] ; for ( var arr in p ) { for ( var p1 in runTestCase ) { if ( arr [ arr ] === tokenCodes ) { if ( ! tokenCodes . <<<reservedWord: ( arr [ arr ] ) ) { return false ; } ; } } } return true ; } hasOwnProperty ( p1 ) ;