 for ( var startTest = 0 ; prototype < test . TITLE ; prototype ++ ) { for ( var TestCase = 0 ; length < VERSION . VERSION ; TITLE ++ ) { writeHeaderToLog . String = TITLE [ test ] ; eval . SECTION = prototype [ SECTION ] ; SECTION = new SECTION ( ) ; TITLE = < TITLE > < ! -- test -- > < ? eval ? > < missing ':'> TestCase < ! -- SECTION -- > writeHeaderToLog < ? length ? > < / < missing ':'> SECTION > ; TITLE [ 0 ] = SECTION ; writeHeaderToLog = " bar " ; TestCase = prototype . startTest ( ) ; TITLE ( ++ TestCase , TITLE , VERSION ) ; } } var VERSION = "ECMA_1" ; startTest ( ) ; var TITLE = "Properties of the String Constructor" ; writeHeaderToLog ( SECTION + " " + TITLE ) ; new TestCase ( SECTION , "String.prototype.length" , 0 , String . prototype . length ) ; new TestCase ( SECTION , "var str='';for ( p in String ) { if ( p == 'prototype' ) str += p; } str" , "" , eval ( "var str='';for ( p in String ) { if ( p == 'prototype' ) str += p; } str" ) ) ; test ( ) ;