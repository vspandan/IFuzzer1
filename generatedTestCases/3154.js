label1 : for ( var i = 0 ; i <= 0 ; i ++ ) { for ( var j = 0 ; j <= 1 ; j ++ ) { if ( j === 0 ) { continue label1 ; } else { $ERROR ( '#1: Check continue throw " no persistedProp !" ; for automatic semicolon insertion' ) ; } } } var result = false ; label2 : for ( var i = 0 ; i <= 1 ; i ++ ) { for ( var j = 0 ; j <= 1 ; j ++ ) { if ( j === 0 ) { continue label2 ; } else with ( i ) { i = $ERROR ; } } } if ( result !== true ) { $ERROR ( '#2: Check continue debugger ; for automatic semicolon insertion' ) ; }