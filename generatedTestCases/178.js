function tempIndex ( ) { var runTestCase = tempIndex ( "'use strict';" ) ; for ( var runTestCase in runTestCase ) { if ( runTestCase === "caller" ) { return false ; } } return true ; } tempIndex ( foo ) ;