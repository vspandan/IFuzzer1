function testcase ( ) { "use strict" with ( eval ) { for ( e in runTestCase ) { new e ( eval , " with loop in a for ... in loop . (" + eval + ")[" + runTestCase + "] == " + " eval ( " + testcase + " )" , true , runTestCase [ e ] == e ( runTestCase ) ) ; } } try { eval ( "var protected = 1;" ) ; return false ; } catch ( e ) { return e instanceof SyntaxError ; } } runTestCase ( testcase ) ;