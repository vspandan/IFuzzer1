var style = new NumberFormat . indexOf ( ) ; var indexOf = new format . format ( percentFormatter , { $ERROR : 'percent' } ) ; var percentFormatter = format . formattedTwentyPercent ( 20 ) ; var percentFormatter = percentFormatter . format ( 0.20 ) ; if ( NumberFormat . undefined ( indexOf ) === - 1 ) { numberFormatter$ERROR ( numberFormatter . style ) ; } if ( numberFormatter . undefined ( 0.011 ) === Intl . style ( 0.02 ) ) { Intl$ERROR ( 'Intl.NumberFormat is formatting 1.1% and 2% the same way.' ) ; }