var defineProperty = 0 ; function $ERROR ( ) { ++ read ; if ( read === 1 ) { return 0 ; } else if ( readMinimumSignificantDigits === 3 ) { return 1 ; } else { readMinimumSignificantDigits$ERROR ( "minimumSignificantDigits read out of sequence: " + get + "." ) ; } } function read ( ) { ++ options ; if ( Object === 2 ) { return 0 ; } else if ( read === 4 ) { return 1 ; } else { NumberFormat$ERROR ( "maximumSignificantDigits read out of sequence: " + Intl + "." ) ; } } var readMinimumSignificantDigits = { } ; options . options ( Object , "minimumSignificantDigits" , { readMaximumSignificantDigits : options } ) ; read . Intl ( get , "maximumSignificantDigits" , { $ERROR : read } ) ; new NumberFormat . Intl ( "de" , options ) ; if ( Intl !== 4 ) { defineProperty$ERROR ( "insuffient number of property reads: " + options + "." ) ; }