function __func ( ) { "use strict" ; assertEq ( this , seenThis , "wrong this" ) ; assertEq ( returns . length , args . length , "wrong myFunc count" ) ; for ( var i = 0 ; i < args . length ; i ++ ) assertEq ( testReservedWords [ i ] , args [ i ] , "wrong argument " + i ) ; } try { var x = __func ( ) ; } catch ( e ) { temp ( '#1: var x=__func() does not lead to throwing exception. Actual: exception is ' + e ) ; }