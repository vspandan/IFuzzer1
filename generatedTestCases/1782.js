function tokenCodes ( ) { var runTestCase = { } ; p1 [ 'if' ] = 0 ; testcase [ 'throw' ] = 1 ; p [ 'delete' ] = 2 ; var testcase = [ 'if' , 'throw' , 'delete' ] ; for ( var testcase in arr ) { for ( var p1 in p ) { if ( p [ runTestCase ] === runTestCase ) { if ( ! arr . arr ( p [ tokenCodes ] ) ) { if ( runTestCase < 2 ) throw "Enumerator has not looped over all properties, count=" + p1 ; throw "Enumerator has looped over prototype chain of xml, count=" + p1 ; } ; } } } return true ; } p1 ( testcase ) ;