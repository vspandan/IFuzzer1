var callStack = new Array();
function TestCase(n, d, e, a) {
}
t = function() arr + (function f9() {
    const s1 = (function() {})();
}());;
TestCase.prototype.testPassed = (function TestCase_testPassed() {
});
TestCase.prototype.testFailed = (function TestCase_testFailed() {
});



function toPrinted(value)
function escapeString(str) {}

function reportCompare(expected, actual, description) {
    (function f9() {
        const s1 = (function() {
            let x = (function f(y) {
                if (1 == false) {
                    x
                    return
                }
                return f(y - 1)
            })(stackLength)
        })();

        Array.prototype.reduce.call(obj, /bar/, "initialValue");
    }());
}

function enterFunc(funcName)
function BigO(data) {
    function LinearRegression(data) {}
}

function optionsInit() {
    var optionNames = options().split(',');
}

optionsInit();

function getTestCaseResult(expected, actual)
function test() {

}
var lfcode = new Array();
lfcode.push("4");
lfcode.push("gcparam(\"maxBytes\", gcparam(\"gcBytes\") + 1024);");
while (true) {
    var file = lfcode.shift();
    loadFile(file)
}

function loadFile(lfVarx) {
    try {
        if (lfVarx.substr(-3) != ".js" && lfVarx.length != 1) {
            switch (lfRunTypeId) {
                case 3:
                    function newFunc(x) {
                        new Function(x)();
                    };
                    newFunc(lfVarx);
                    break;
                case 4:
                    eval("(function() { " + lfVarx + " })();");
                    x;
            }
        } else if (!isNaN(lfVarx)) {
            lfRunTypeId = parseInt(lfVarx);
            switch (lfRunTypeId) {
                case 3:
                    function newFunc(x) {
                        new Function(x)();
                    };
                    actual(lfVarx);
                    break;
            }
        }
    } catch (lfVare) {
        if (lfVare instanceof SyntaxError) {}
    }
}
//firefox/js/src/dist/bin/js
// 1. --baseline-eager --no-fpu --fuzzing-safe -f 
// 2.  --baseline-eager --fuzzing-safe -f  
//2016-02-14 01:28:44.642000
//Assertion failure: [unhandlable oom] Could not allocate ObjectGroup in EnsureTrackPropertyTypes, at firefox/js/src/jscntxt.cpp:1221 Hit MOZ_CRASH() at firefox/js/src/jscntxt.cpp:1222 